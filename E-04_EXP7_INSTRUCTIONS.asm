; SHREEYA PIMPLE
; E-04
; EXP 7 INSTRUCTIONS


	ORG	0000H
	LJMP	MAIN

	ORG	0040H
MAIN:

;ARETHMETIC INSTRUCTIONS

;ADD
	MOV	A, #0FH
	ADD	A, #0BH

;SUBB
	MOV	A, #55H
	MOV	R1, #29H
	SUBB	A, R1

;ADD MULTIPLE OPERATIONS
	MOV	R0, #40H
	MOV	R2, #5H
	CLR	A
	MOV	R7, A
AGAIN:	ADD	A, @R0
	JNC	NEXT
	INC	R7
NEXT:	INC	R0
	DJNZ	R2, AGAIN

;ADDC
	CLR	C

	MOV	A, #60H
	ADD	A, #8DH
	MOV	R6, A
	MOV	A, #3CH
	ADDC	A, #3BH
	MOV	R7, A

;A
	MOV	A, #60H
	INC	A

;DA
	MOV	A, #60H
	MOV	B, #25H
	ADD	A, B
	DA	A


;LOGOCAL INSTRUCTIONS
;ANL
	MOV	A, #0FFH
	MOV	R0, #66H
	ANL	A, R0

;ORL
	MOV	A, #066H
	MOV	R0, #99H
	ORL	A, R0

;XRL
	MOV	A, #0FFH
	MOV	R0, #66H
	XRL	A, R0

;CRL 
	MOV	A, #0FFH
	CLR	A

;CPL
	MOV	A, #066H
	CPL	A

;RL
	MOV	A, #080H
	RL	A

;RCL
	SETB	C
	MOV	A, #07EH
	RLC	A

;BOOLEAN MULTIPLICATIONS 

;SETB
;JB
	SETB	P1.5
HERE:	JB	P1.5, HERE
	MOV	P2, #55H

;JNB
	SETB	P1.4
HELLO:	JNB	P1.4, HELLO
	MOV	P2, #55H

;JBC 
	JNB	ACC.7, RIGHT
	MOV	P1, A

RIGHT:

;JNC
	CLR	A
	MOV	R3, A
	ADD	A, #0F6H
	JNC	OVER1
	INC	R3
OVER1:	ADD	A, #98H



;PROGRAM BRANCHING

;DJNZ
	CLR	A
	MOV	R2, #20H
BACK:	INC	A
	MOV	P1, A
	DJNZ	R2, BACK

;JNZ 
	MOV	R5, 16
	MOV	R3, #0
	MOV	R1, #40H
ABOVE:	MOV	A, @R1
	JNZ	HII
	INC	R3
HII:	INC	R1
	DJNZ	R5, ABOVE

;ACALL
;SJMP
;JZ
	MOV	R0, #50H
LEFT:	MOV	A, @R0
	JZ	EXIT
	MOV	P1, A
	INC	R0
	SJMP	LEFT
EXIT:

;DATA TRANSFER 

;MOV
	MOV	A, 60H
	MOV	A, 45H

;MOVC

	MOV	A, R3
	INC	A
	MOVC	A, @A+PC
	RET
SQUR:	DB	0, 1, 4, 9, 16, 25, 36, 49, 64, 81
	END

;MOVX 
	MOVX	A, @DPTR

	MOVX	@DPTR, A

;PUSH
;POP
;XCHD
;XCH

	END
